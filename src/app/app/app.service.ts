import { Injectable } from '@angular/core';
import { dia } from '@joint/core';

@Injectable({ providedIn: 'root' })
export class ShapeService {
  // createCircle(
  //   x: number,
  //   y: number,
  //   width: number,
  //   height: number,
  //   label: string,
  //   graph: dia.Graph
  // ): dia.Element {
  //   const circle = new dia.Element();
  //   circle.position(x, y);
  //   circle.resize(width, height);
  //   circle.attr('label', { text: label });
  //   circle.addTo(graph);
  //   return circle;
  // }

  // Other shape utilities...
}
